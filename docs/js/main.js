const zoomist = new Zoomist('[data-zoomist]', {
  // fill: 'cover',
  // src: 'data-zoomist-src',
  // wheelable: true,
  // draggable: true,
  // bounds: true,
  // zoomRatio: 0.1,
  maxRatio: false
})

console.log(zoomist)
