export default {
  /**
   * get container (element) data
   * @returns {Object}
   */
  getContainerData() {
    return this.data.container
  },

  /**
   * get image data
   * @returns {Object}
   */
  getImageData() {
    return this.data.image
  },
}